CREATE TABLE USERS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USERNAME VARCHAR2(50) NOT NULL UNIQUE,
    EMAIL VARCHAR2(100) NOT NULL UNIQUE,
    PASSWORD_HASH VARCHAR2(255) NOT NULL,
    FULL_NAME VARCHAR2(120) NOT NULL,
    PHONE_NUMBER VARCHAR2(25),
    PROFILE_IMAGE_URL VARCHAR2(255),
    STATUS VARCHAR2(16) NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    LAST_LOGIN_AT TIMESTAMP,
    DELETED NUMBER(1) DEFAULT 0 NOT NULL
);

CREATE TABLE ROLES (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ROLE_NAME VARCHAR2(64) NOT NULL UNIQUE,
    DESCRIPTION VARCHAR2(255)
);

CREATE TABLE USER_ROLES (
    USER_ID NUMBER NOT NULL,
    ROLE_ID NUMBER NOT NULL,
    CONSTRAINT PK_USER_ROLES PRIMARY KEY (USER_ID, ROLE_ID),
    CONSTRAINT FK_USER_ROLES_USER FOREIGN KEY (USER_ID) REFERENCES USERS(ID),
    CONSTRAINT FK_USER_ROLES_ROLE FOREIGN KEY (ROLE_ID) REFERENCES ROLES(ID)
);

CREATE TABLE CMS_PAGES (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TITLE VARCHAR2(180) NOT NULL,
    SLUG VARCHAR2(200) NOT NULL UNIQUE,
    PAGE_TYPE VARCHAR2(40) NOT NULL,
    CONTENT CLOB NOT NULL,
    META_TITLE VARCHAR2(200),
    META_DESCRIPTION VARCHAR2(500),
    STATUS VARCHAR2(16) NOT NULL,
    PUBLISHED_AT TIMESTAMP,
    CREATED_BY VARCHAR2(255),
    UPDATED_BY VARCHAR2(255),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE CMS_MENUS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR2(100) NOT NULL,
    URL VARCHAR2(120) NOT NULL,
    DISPLAY_ORDER NUMBER DEFAULT 0 NOT NULL,
    ACTIVE NUMBER(1) DEFAULT 1 NOT NULL,
    MENU_GROUP VARCHAR2(60),
    CREATED_BY VARCHAR2(255),
    UPDATED_BY VARCHAR2(255),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE CMS_CONTENT_BLOCKS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    PAGE_ID NUMBER NOT NULL,
    BLOCK_TYPE VARCHAR2(40) NOT NULL,
    TITLE VARCHAR2(200),
    BODY CLOB,
    IMAGE_URL VARCHAR2(255),
    DISPLAY_ORDER NUMBER DEFAULT 0 NOT NULL,
    ACTIVE NUMBER(1) DEFAULT 1 NOT NULL,
    CREATED_BY VARCHAR2(255),
    UPDATED_BY VARCHAR2(255),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    CONSTRAINT FK_CMS_BLOCK_PAGE FOREIGN KEY (PAGE_ID) REFERENCES CMS_PAGES(ID)
);

CREATE TABLE EVENTS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TITLE VARCHAR2(200) NOT NULL,
    SLUG VARCHAR2(220) NOT NULL UNIQUE,
    DESCRIPTION CLOB NOT NULL,
    LOCATION VARCHAR2(255),
    EVENT_START TIMESTAMP NOT NULL,
    EVENT_END TIMESTAMP,
    IMAGE_URL VARCHAR2(255),
    STATUS VARCHAR2(16) NOT NULL,
    FEATURED NUMBER(1) DEFAULT 0 NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE SERMONS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TITLE VARCHAR2(200) NOT NULL,
    SPEAKER VARCHAR2(120) NOT NULL,
    SERMON_DATE DATE NOT NULL,
    DESCRIPTION CLOB,
    YOUTUBE_URL VARCHAR2(255),
    AUDIO_URL VARCHAR2(255),
    SCRIPTURE_REFERENCE VARCHAR2(255),
    SERIES_NAME VARCHAR2(120),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE LIVESTREAMS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TITLE VARCHAR2(200) NOT NULL,
    YOUTUBE_EMBED_URL VARCHAR2(255) NOT NULL,
    DESCRIPTION CLOB,
    ACTIVE NUMBER(1) DEFAULT 0 NOT NULL,
    SCHEDULED_START TIMESTAMP,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE ANNOUNCEMENTS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TITLE VARCHAR2(200) NOT NULL,
    MESSAGE CLOB NOT NULL,
    START_DATE DATE,
    END_DATE DATE,
    ACTIVE NUMBER(1) DEFAULT 1 NOT NULL,
    PRIORITY NUMBER DEFAULT 0 NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE PRAYER_REQUESTS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR2(120) NOT NULL,
    EMAIL VARCHAR2(150) NOT NULL,
    PHONE VARCHAR2(25),
    MESSAGE CLOB NOT NULL,
    CONFIDENTIAL NUMBER(1) DEFAULT 0 NOT NULL,
    STATUS VARCHAR2(16) NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE CONTACT_MESSAGES (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR2(120) NOT NULL,
    EMAIL VARCHAR2(150) NOT NULL,
    SUBJECT VARCHAR2(200) NOT NULL,
    MESSAGE CLOB NOT NULL,
    STATUS VARCHAR2(16) NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE NEWS_ARTICLES (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TITLE VARCHAR2(220) NOT NULL,
    SLUG VARCHAR2(240) NOT NULL UNIQUE,
    CONTENT CLOB NOT NULL,
    EXCERPT VARCHAR2(1000),
    IMAGE_URL VARCHAR2(255),
    AUTHOR_ID NUMBER,
    STATUS VARCHAR2(16) NOT NULL,
    PUBLISHED_AT TIMESTAMP,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE GALLERY_ITEMS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TITLE VARCHAR2(200) NOT NULL,
    DESCRIPTION VARCHAR2(1000),
    IMAGE_URL VARCHAR2(255) NOT NULL,
    ALBUM_NAME VARCHAR2(120),
    DISPLAY_ORDER NUMBER DEFAULT 0 NOT NULL,
    ACTIVE NUMBER(1) DEFAULT 1 NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE SITE_SETTINGS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    SETTING_KEY VARCHAR2(120) NOT NULL UNIQUE,
    SETTING_VALUE CLOB,
    DESCRIPTION VARCHAR2(500),
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);
